import Background from './plus-background/background.js';
import { SafeAreaView } from 'react-native';

function App() {
  return (
    <SafeAreaView style={{flex: 1}}>
      <Background />
    </SafeAreaView>
  );
}

export default App;
